---
- hosts: all
  vars:
    workstation_user: apaz
    isOSX: true
    task_dirs: 
      - osx
      - bashrc
      - editorconfig
      - git
      - vim
      - code.d
      - tmux
      - kubernetes
      - node
      - firefox
      - terminal
      - intellij
      - vscode
  tasks:
    - include: "{{ item }}/tasks.yml"
      with_items: "{{ task_dirs }}"
