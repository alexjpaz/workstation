#!/bin/bash

dnf -y install gnome-tweak-tool

su -c 'git clone https://github.com/negesti/gnome-shell-extensions-negesti ~/.local/share/gnome-shell/extensions/putWindow@clemens.lab21.org' $WORKSTATION_USER

